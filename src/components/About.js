import React from 'react';

const About = ( props ) => {

    return(
        <div className="main-content">
            <h2>{ props.title }</h2>
            <p>The front ent course director lists many of the courses we teach on HTML</p>
        </div>
    );

};

export default About;